node_modules/
dist-ssr/
coverage/
*.log

# Keep dist/ committed so Hostinger can serve it without a build step
# dist/

# Local env files — .env.production is safe and IS committed
.env
.env.local
.env.*.local

# Editor
.vscode/
.idea/
.DS_Store
Thumbs.db
